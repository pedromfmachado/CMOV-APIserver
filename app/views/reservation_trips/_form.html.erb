<%= form_for(@reservation_trip) do |f| %>
  <% if @reservation_trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reservation_trip.errors.count, "error") %> prohibited this reservation_trip from being saved:</h2>

      <ul>
      <% @reservation_trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reservation %><br />
    <%= f.text_field :reservation %>
  </div>
  <div class="field">
    <%= f.label :trip %><br />
    <%= f.text_field :trip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
